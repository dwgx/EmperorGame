This folder hosts the new Vue (Vite) frontend for EmperorGame.

- `npm install` to install dependencies.
- `npm run dev` for local development.
- `npm run build` outputs production files into `../src/main/web` so the Java server can serve them directly.
